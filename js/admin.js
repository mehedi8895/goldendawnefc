(async()=>{let p=await GDFC.approved();if(!p||!['owner','president','moderator'].includes(p.role)){content.innerHTML='<p>Staff access required.</p>';return}let m=await sb.from('profiles').select('id,full_name,email,status').eq('status','pending');content.innerHTML=`<div class="panel"><h3>Pending members</h3>${(m.data||[]).map(x=>`<div class=item><b>${GDFC.esc(x.full_name)}</b><button class="btn gold" data-approve="${x.id}">Approve</button></div>`).join('')||'<p>No pending members.</p>'}</div>`;document.querySelectorAll('[data-approve]').forEach(b=>b.onclick=async()=>{alert('Use the supplied Edge Function endpoint for privileged approval.');})})()
